# 操作手册

## 访问页面

- 本地开发：`http://localhost:4819/yanlili`
- 正式环境：`https://fluffy-cupcake.cn/yanlili`

根路径 `/` 会自动跳转到 `/yanlili`。

## 使用按钮

点击或轻触页面中央的 GIF 按钮，每次都会出现一行“按按钮，想哥哥+1”，文字向上漂浮后渐隐。连续点击可以同时显示多行文字。

使用键盘时，先通过 Tab 聚焦按钮，再按 Enter 或空格即可触发。页面会遵从系统“减少动态效果”设置。
